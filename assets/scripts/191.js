const hobbies = ["Sports", "Cooking", "Cooking"];

// Tìm theo value và trả về vịt trí index đầu tiên đc tìm thấy
const option = hobbies.indexOf("Cooking");
console.log(option);

// Tìm index cuối cùng
const result = hobbies.lastIndexOf("Cooking");
console.log(result);
